{
  "name": "@agentio/agent-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Agent runtime — orchestrate runs, policies, skills, and drivers into a complete agent loop",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "bun test __tests__/*.test.ts"
  },
  "dependencies": {
    "@agentio/protocol": "workspace:*",
    "@agentio/kernel-run": "workspace:*",
    "@agentio/kernel-policy": "workspace:*",
    "@agentio/kernel-schema": "workspace:*",
    "@agentio/kernel-state": "workspace:*",
    "@agentio/kernel-artifact": "workspace:*",
    "@agentio/skills-runtime": "workspace:*",
    "@agentio/driver-exec": "workspace:*",
    "@agentio/driver-mcp": "workspace:*",
    "ai": "^6.0.0",
    "@ai-sdk/anthropic": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
