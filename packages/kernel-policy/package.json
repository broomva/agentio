{
  "name": "@agentio/kernel-policy",
  "version": "0.1.0",
  "private": true,
  "description": "Policy engine — evaluate rules, enforce budgets, gate approvals",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "bun test __tests__/*.test.ts"
  },
  "dependencies": {
    "@agentio/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
