{
  "name": "@agentio/kernel-state",
  "version": "0.1.0",
  "private": true,
  "description": "State manager — aggregate run, artifact, and event state into a coherent view",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --test __tests__/*.test.ts"
  },
  "dependencies": {
    "@agentio/protocol": "workspace:*",
    "@agentio/kernel-run": "workspace:*",
    "@agentio/kernel-artifact": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
