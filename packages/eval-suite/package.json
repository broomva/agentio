{
  "name": "@agentio/eval-suite",
  "version": "0.1.0",
  "private": true,
  "description": "Evaluation suite — benchmark, score, and regression-test agent runs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --test __tests__/*.test.ts"
  },
  "dependencies": {
    "@agentio/protocol": "workspace:*",
    "@agentio/kernel-run": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
