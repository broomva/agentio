{
  "name": "@agentio/chat-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Chat bot — conversational agent interface for messaging platforms",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "bun test __tests__/*.test.ts"
  },
  "dependencies": {
    "@agentio/agent-runtime": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
